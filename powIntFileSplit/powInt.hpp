#ifndef POWINT_H //se non definita l'etichetta
#define POWINT_H //allora definiscila

/*file di header --> documentazione, ciò che ha il cliente*/

int powInt (int base, int exp);

#endif //finisce l'if

//righe con # non è vero codice, ma sono le prime che vengono analizzate dal compilatore (istruzioi al pre-processore)
//successivamente il file viene letto senza queste righe